<h2><%= year %></h2>
<ul class="archives">
  <% posts.forEach(post => { %>
    <li>
      <%- link_to(post.path, _.escape(post.title)) %>
      <time datetime="<%= post.date.format() %>">
        <%= post.date.format('MM/DD') %>
      </time>
    </li>
  <% }) %>
</ul>
